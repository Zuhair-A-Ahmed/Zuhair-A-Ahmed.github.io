# Preview

This branch is intended for review before deployment.

To preview locally:

```bash
python -m http.server 8000
```

Open `http://localhost:8000` and review the home page and `/404.html` on desktop and mobile viewport sizes.
